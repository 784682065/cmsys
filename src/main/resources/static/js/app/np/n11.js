single = {    init: function () {        tableType = "prove";        inputType = "text";        needUserInfo = 1;        addOrView = getCookie("addOrView");        if (addOrView == "add") {            $(".back").attr("data", "add");            $("#addDiv").show();            $("#views").hide().remove();            nps.initAdd();        } else if (addOrView == "view") {            $(".back").attr("data", "detail");            $("#addDiv").hide().remove();            $("#views").show();            nps.initDetail();        }    },    npsView: function (data) {        if(data.businessData&&data.businessData!="") {            var value = data.businessData;            $("#userName").text(value.APPLYNAME);            $("#userCard").text(value.IDCARD);        }    },    //重新发起时，可以查看和修改之前的数据    resaveNps: function (data) {        if (data.businessData && data.businessData != "") {            var value = data.businessData;            $("#userName").text(value.APPLYNAME);            $("#userCard").text(value.IDCARD);        }    },    addCheck: function () {  //发起业务时各表单需验证的情况略不相同        commons.alertMsg(formats.checkFormat());    },add:function() { //表单提交        var datas = {            "applyName": $("#userName").text(),            "business": businessId, "idCard": $("#userCard").text()        };        $(".locked").fadeIn();        $(".modalBg").hide();        commons.saveData(datas);    }};single.init();