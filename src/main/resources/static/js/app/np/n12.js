single = {    init: function () {        tableType = "repairs";        inputType = "text";        needUserInfo = 1;        addOrView = getCookie("addOrView");        if (addOrView == "add") {            $(".back").attr("data", "add");            nps.initAdd();            $("#APPLYTIME").val(formats.timeFormat("now", "year"));            $("#APPLYTIMEp").removeClass("dateBg");        } else if (addOrView == "view") {            $(".back").attr("data", "detail");            nps.initDetail();        }    },    npsView: function (data) {        var itemHtml = "";        if (data && data != "") {            var value = data;            $("#userName").html(value.APPLYNAME);            $("#userJob").html(value.UNIT);            $("#applyEventp").html(value.APPLYEVENT);            $("#applyAddp").html(value.APPLYADD);            $("#applyReasonp").html(value.APPLYREASON);            $("#APPLYTIMEp").html(value.APPLYTIME);        }    },    //重新发起时，可以查看和修改之前的数据    resaveNps: function (data) {        if (data && data != "") {            var value = data;            $("#userName").html(value.APPLYNAME);            $("#userJob").html(value.UNIT);            $("#applyEvent").text(value.APPLYEVENT);            $("#applyAdd").text(value.APPLYADD);            $("#applyReason").text(value.APPLYREASON);            $("#APPLYTIME").val(value.APPLYTIME);        }    },    addCheck: function () {  //发起业务时各表单需验证的情况略不相同        commons.alertMsg(formats.checkFormat());    },add:function() { //表单提交        var datas = {            "applyAdd": $("#applyAdd").val(),            "applyEvent": $("#applyEvent").val(),            "applyReason": $("#applyReason").val(),            "applyTime": $("#APPLYTIME").val(),            "applyName": $("#userName").text(),            "unit": $("#userJob").text(),            "business": businessId        };        $(".locked").fadeIn();        $(".modalBg").hide();        commons.saveData(datas);    }};single.init();